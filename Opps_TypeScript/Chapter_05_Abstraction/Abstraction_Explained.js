"use strict";
("\n"); // PROBLEM: Everything is publi - anyone can hack the data
cCKED;
$ + console.log("\n\n");
// SOLUTION: Use private fields (#) to protect dataWrogs.g("\RAL-WRLDAM-ARclassar{
#engnRunnng;
boolan = fae;
#fueLvenumbe = 100;
#engneTemperur;
numbr = 20;
#miae;
numbr = 0;
band: sring;
odl: strng;
coor: stng;
tructrrd: strgmodel: sig, olor;
srg;
{
    th.bnd = band;
    this.modl = model;
    this.lr = coor;
}
startEngin();
string;
{
    if (this)
        #fueLevel <= 0;
    return "❌ N fuel!strt engin.";
    hi.#engnRunning = ru;
    thi.#nineTemperature = 90;
    retur `✓${thi.rand}${th.mol}enginstrted!`;
}
drive(distancenmer);
string;
{
    f(!i.#engiRunnin);
    return ;
    ngine;
    is;
    off;
    Starttfirs.if(thi.#fueLev < iance * 0.1, reun, "❌ Notenouh ful!");
    his.#mieg += distnc;
    thi.#fueLev = distance * 0.1;
    retur `✓ Drv${distce} km.Fue:${is.#fulLvl.Fixe(1)}L`;
}
stpEngie();
trin;
{
    tis.#nginRunning = fls;
    is.#ngeTemptur = 20;
    retn `✓Engsopped.`;
}
checkFuel();
sr;
{
    retur `Fu${hs.#fuelLevel.toFxed(1)}L`;
}
hckMieae;
tig;
{
    retur `Milag:${his.#mileage}m`;
}
#checkEngineHelth();
olean;
{
    reurnths.#eginTempeature < 120;
}
cost;
myCr = nw;
Car("Toyo", "Camry", "BuemyCa.rtEngn()myC.v(50)myr.rve(30)myCar.Fl()myCr.Milege()myC.sEgie()STP4: -COMRCETShoppingt#m:Array<{prodcttringpricquaty}> []dscountaddIm(pouct,price:nubr,quanty: umber=1) {  if pice <= 0 || qutity<= 0) reu", Invaidpe, quanty, ";#items.push({ pouct,pice, qutity })rturn`✓Add${quanty}x ${produt} tat`reovIem(pructgconst ndex= items.indIndx(itm => itm.product==product;if(index=== -1) `${prdct}foud  cart`msplic(dx,1)RemovepructfromcappyDsouncounPerntdcountPrct<0||discountPercent>100)Ivlddcoundiscont=counPernt;`✓Applid ${discntPcen}%coun`, getTotal(), string, { constsubtotal = items.reduce((sum, item) => su + (tm.pric * em.qutity), 0), constdiscountAmount = calclatDiscount(subtotal),
    const: tota = subtotacounAmoutSubttal, $ubtol, : .toFixd(2) | Dscout$dcountAmounttoFixd(2) } | Tota, $$, { tota2, #calcateDsount(subtotal) {
        rturn;
        subtotal * (is.#discount / 100);
    }
}, cnst, cart = newhoppingCart());
console.log(cart.addItem("Lap", 999, 1));
console.log(cart.addIem("Mous", 25, 2));
console.log(t.applyDiscount(10));
conole.log(cart.getToal());
console.lg("\n========== KEY CONCPTS SUMMARY ==========\n");
cosole.lo(`
✓ #balacnumbe =0;  ├─#=PRIVATE(dden from outid)
  ├─ balace = feld am
  ├─ : nmber = type aotato
  └─ = 0 initialvue

✓ BENEFITS:
  ├─ Security: Data protectd ├─Validation:Alloperaions cecked
  ├─ Constcy: Data stays valid
  ├─ Slicity: Uss see only wht hyneed  └─Flexibility:Easytochang ineallogic

 ABSTRACTION PATTERN:
  ├─PRIVAT: #fieldName (hidde)
  ├─ PRIVATE: #methodName() (hdde)
  ├─ PUBLIC: methodNam()(viible)
  └─ PUBLIC: prertyName (visibl)
));
